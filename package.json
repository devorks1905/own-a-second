{
  "name": "own-a-second",
  "version": "1.0.0",
  "private": true,
  "description": "Own one second of the world — a global clock where seconds are sold.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "puppeteer": "^24.43.1"
  }
}
